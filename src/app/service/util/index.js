import {isObject,merge, mergeMulti, urlTpl} from './base.js'
import flatobj from './flatobj.js'
import intersection from './intersection.js'
import eventUtil from './eventUtil.js'
var util = {
    isObject,
    merge,
    mergeMulti,
    urlTpl,
    flatobj,
    intersection,
    eventUtil
}

export default util

